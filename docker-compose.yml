services:
  flow-manager:
    build: .
    container_name: flow-manager
    ports:
      - "8080:8080"
    volumes:
      - ./config.yaml:/app/config.yaml
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - GIN_MODE=release
